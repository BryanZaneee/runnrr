No headings at all. Just a single short paragraph describing a fictional
release: version 0.0.1 ships with the chunker, manifest, and storage layer
needed by every downstream phase. This file deliberately has no markdown
structure so chunker tests can verify the headings-empty path.
